# dependencies
node_modules/

# build output
dist/
.astro/
.vercel/

# environment
.env
.env.local
.env.production
.env.*.local

# editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# internal docs (kept local)
CLAUDE.md
.claude/
dscrbroker-design.md
WHERE-IS-GOOGLE-ADS.md
DSCRbrokers-apr2.vtt
context/
scripts/
# The postbuild cache-header fix MUST ship: package.json's build script runs it,
# so an ignored copy breaks every Vercel deploy with Cannot find module.
!scripts/fix-vercel-cache-headers.mjs
# IndexNow submitter must ship too (run manually after page-adding deploys).
!scripts/indexnow-submit.mjs
CHANGE-LOG-*.md

# Local dev server log
dev.log

.vercel
.env*
